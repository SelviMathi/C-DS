// Online C compiler to run C program online
#include <stdio.h>
int main() {
int noe;
scanf("%d",&noe);

int arr[noe];

for(int index=0;index<noe;index++)
scanf("%d",&arr[index]);

for(int index=noe;index>=0;index--)
printf("%d ",arr[index]);


    return 0;
}
